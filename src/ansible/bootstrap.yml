- hosts: jumpy
  tasks:
    - name: Run mount-stage3.sh script.
      script: scripts/mount-stage3.sh
      environment:
        LUKSPASS: "{{ lookup('password', 'credentials/luks.' + inventory_hostname) }}"
